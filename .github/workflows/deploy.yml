name: Deploy App Provedor to Production

on:
  push:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: juniorssilvaa/app_provedor

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: app_provedor_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install dj-database-url psycopg2-binary

      - name: Run tests
        env:
          SECRET_KEY: test-secret-key
          DEBUG: True
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/app_provedor_test
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cd backend
          python manage.py migrate
          python manage.py test

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build & push BACKEND
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:${{ github.sha }}

      - name: Build & push WEBHOOK
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-webhook:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-webhook:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code (to get docker-compose.yml)
        uses: actions/checkout@v4

      - name: Deploy to Portainer
        env:
          PORTAINER_URL: https://app-provedor.niochat.com.br
          PORTAINER_API_KEY: ${{ secrets.PORTAINER_API_KEY }}
          BACKEND_IMAGE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:latest
          WEBHOOK_IMAGE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-webhook:latest
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          echo "🔍 Verificando conexão com Portainer..."
          echo "Fetching stacks from $PORTAINER_URL/api/stacks..."
          STACKS=$(curl -s -H "X-API-Key: $PORTAINER_API_KEY" "$PORTAINER_URL/api/stacks")
          
          # Debug: Mostrar a resposta crua para entender o erro
          echo "DEBUG: Portainer Response Body:"
          echo "$STACKS"
          
          # Verifica se a resposta é um array JSON (começa com [)
          if [[ ! "$STACKS" =~ ^\[ ]]; then
             echo "❌ Erro: A resposta do Portainer não é uma lista de stacks válida."
             echo "Verifique se a PORTAINER_API_KEY está correta e tem permissões."
             exit 1
          fi

          STACK_ID=$(echo "$STACKS" | jq -r '.[] | select(.Name=="app_provedor") | .Id')
          
          # Se a stack não existir, você pode precisar criar ou apenas logar o erro
          if [ "$STACK_ID" == "null" ] || [ -z "$STACK_ID" ]; then
            echo "❌ Stack 'app_provedor' não encontrada no Portainer!"
            exit 1
          fi
          
          echo "✅ Stack encontrada: ID=$STACK_ID"
          
          # Verificar se a rede appNet existe, se não, criar
          echo "🔍 Verificando se a rede 'appNet' existe..."
          NETWORKS=$(curl -s -H "X-API-Key: $PORTAINER_API_KEY" "$PORTAINER_URL/api/endpoints/1/docker/networks")
          NETWORK_EXISTS=$(echo "$NETWORKS" | jq -r '.[] | select(.Name=="appNet") | .Id')
          
          if [ -z "$NETWORK_EXISTS" ] || [ "$NETWORK_EXISTS" == "null" ]; then
            echo "📡 Rede 'appNet' não encontrada. Criando rede..."
            CREATE_NETWORK_RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
              -H "X-API-Key: $PORTAINER_API_KEY" \
              -H "Content-Type: application/json" \
              --data '{"Name":"appNet","Driver":"overlay","Attachable":true,"Scope":"swarm"}' \
              "$PORTAINER_URL/api/endpoints/1/docker/networks/create")
            
            CREATE_NETWORK_CODE=$(echo "$CREATE_NETWORK_RESPONSE" | tail -n1)
            if [ "$CREATE_NETWORK_CODE" -eq 201 ] || [ "$CREATE_NETWORK_CODE" -eq 200 ]; then
              echo "✅ Rede 'appNet' criada com sucesso!"
            else
              echo "⚠️  Aviso: Não foi possível criar a rede 'appNet'. HTTP Code: $CREATE_NETWORK_CODE"
              echo "   A rede pode já existir ou você precisa criá-la manualmente no Portainer."
            fi
          else
            echo "✅ Rede 'appNet' já existe (ID: $NETWORK_EXISTS)"
          fi

          # Atualiza o docker-compose.yml com as novas tags de imagem
          COMPOSE=$(cat docker-compose.yml \
            | sed "s|ghcr.io/juniorssilvaa/app_provedor-backend:latest|${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:${{ github.sha }}|g" \
            | sed "s|ghcr.io/juniorssilvaa/app_provedor-webhook:latest|${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-webhook:${{ github.sha }}|g")
          
          # Escapa o conteúdo do docker-compose para JSON
          ESCAPED=$(echo "$COMPOSE" | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')
          
          echo "🔄 Atualizando stack no Portainer..."
          echo "📦 Imagem Backend: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:${{ github.sha }}"
          echo "📦 Imagem Webhook: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-webhook:${{ github.sha }}"
          
          # Faz a atualização da stack no Portainer
          # IMPORTANTE: O Portainer precisa estar configurado para autenticar no ghcr.io
          # Configure um registry no Portainer: Settings > Registries > Add registry
          # Type: Docker Hub, URL: ghcr.io, Username: juniorssilvaa, Password: [GHCR_TOKEN]
          echo "🔄 Atualizando stack no Portainer (Stack ID: $STACK_ID, Endpoint ID: 1)..."
          RESPONSE=$(curl -s -w "\n%{http_code}" -X PUT \
            -H "X-API-Key: $PORTAINER_API_KEY" \
            -H "Content-Type: application/json" \
            --data "{\"StackFileContent\":\"$ESCAPED\",\"PullImage\":true,\"ForceRecreate\":true}" \
            "$PORTAINER_URL/api/stacks/$STACK_ID?endpointId=1")
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 204 ]; then
            echo "✅ Deploy finalizado com sucesso na stack $STACK_ID"
            echo "📋 Resposta do Portainer: $BODY"
            echo ""
            echo "⚠️  NOTA: Se o Portainer não puxar as novas imagens automaticamente,"
            echo "   verifique se há um registry configurado para ghcr.io no Portainer:"
            echo "   Settings > Registries > Add registry"
            echo "   Type: Docker Hub"
            echo "   URL: ghcr.io"
            echo "   Username: juniorssilvaa"
            echo "   Password: [use o GHCR_TOKEN]"
          else
            echo "❌ Erro ao atualizar stack no Portainer. HTTP Code: $HTTP_CODE"
            echo "📋 Resposta: $BODY"
            exit 1
          fi
